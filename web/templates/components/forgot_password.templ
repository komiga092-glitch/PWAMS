package components

templ ForgotPasswordPage(errorMsg string) {
	<!DOCTYPE html>
	<html lang="en">
		<head>
			<meta charset="UTF-8"/>
			<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
			<title>PWAMS - Forgot Password</title>
			<link rel="stylesheet" href="/static/css/app.css"/>
			<script src="/static/js/vendor/htmx.min.js"></script>
		</head>
		<body>
			<div class="login-page">
				<section class="login-form-side" style="width:100%;max-width:480px;">
					<div class="login-card">
						<h2>Forgot Password</h2>
						<p class="subtitle">Enter your email to receive a reset code.</p>
						if errorMsg != "" {
							<div class="alert alert-danger">{ errorMsg }</div>
						}
						<form
							hx-post="/forgot-password"
							hx-target="body"
							hx-swap="outerHTML"
						>
							<div class="form-group" style="margin-bottom:16px;">
								<label class="form-label" for="email">Email Address</label>
								<input class="form-control" id="email" name="email" type="email" required/>
							</div>
							<button class="btn btn-primary" style="width:100%;" type="submit">Send Reset Code</button>
						</form>
						<div style="text-align:center;margin-top:18px;">
							<a href="/login" style="color:var(--primary);font-size:13px;font-weight:700;">Back to Login</a>
						</div>
					</div>
				</section>
			</div>
		</body>
	</html>
}
